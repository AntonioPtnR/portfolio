import { Box, styled } from "@mui/material";

const StyledFirstBox = styled(Box) (() => ({
    display: 'flex',
    alignItems: 'flex-start',
    justifyContent: 'space-evenly'
}))

export {StyledFirstBox}