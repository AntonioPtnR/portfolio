import { Box, Button, styled } from "@mui/material";

const StyledNavigatorBox = styled(Box) (() => ({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-evenly'
}));

const StyledLogoButton = styled(Button) (() => ({
    
}));

export {
    StyledNavigatorBox,
    StyledLogoButton
}